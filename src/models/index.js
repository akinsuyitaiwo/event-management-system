const User = require('./userModel.js');
const Event = require('./eventsModel.js');
const Attendee = require('./attendeeModel.js');

const models = {
    User, Event, Attendee
}

module.exports = models;